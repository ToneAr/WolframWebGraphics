import type { ReactElement } from "react";

/**
 * @wgx/react-runtime — React surface for the WGX web runtime.
 *
 * Placeholder: the React wrappers are not implemented yet. The build, type,
 * and lint configuration is wired so components can be added under `src/`.
 */
export function WgxRuntime(): ReactElement | null {
	// TODO: mount @wgx/js-runtime against a container ref and dispatch 2D/3D.
	return null;
}

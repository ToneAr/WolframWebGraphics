# The WL prettier plugin is published to a private Wolfram registry.
# The matching _authToken lives in your global ~/.npmrc (never commit it here).
@wrel:registry=http://wrel-resources.wolfram.com:4873
